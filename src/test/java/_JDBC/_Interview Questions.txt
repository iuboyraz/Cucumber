JDBC Nedir ?
Veritabanýný automation yapmama yardýmcý olan kütüphanedir.
JDBC is an API consists of a set of interfaces and classes written in the Java programming Language.
It enables me to access DBMS so that I can manipulate the database automatically.

JDBC yi nasýl kullanýrsýn ?
I send my queries and compare them with the results returning from the web.
To do this;
first of all, set a connection between java and DBMS by entering URL, username, and password
then create my statement which will be used through this connection, and
finally, run my tests and get results, ResultSet.

Sorgularýmý gönderiyorum, gelen sonuçlarla ,web deki sonuçlarý
karþýlaþtýrýyorum. Bunun için;
URL, username, password vererek bir connection oluþtururum.
Bu connection üzerinden sorgularýmý çalýþtýracaðým "statement"oluþtururum.
Sorgularýmý çalýþtýrýp, sonuçlarý yani ResulSet i alýrým.